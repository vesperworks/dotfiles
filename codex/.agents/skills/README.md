# skills

このディレクトリは GNU Stow により `~/.agents/skills/` へリンクされる。

現時点では custom skill を主役にせず、Codex 標準作法を優先する。

必要になった場合のみ、繰り返し使うフォーマット補助を小さな skill として追加する。

- `vwnote`: `ノートにして` 用の Atomic Notes 整形
- `autoloop-bootstrap`: 質問ベースで半自動改善ループを初期化
- `commit-helper`: jj 優先の安全なコミット運用を補助
