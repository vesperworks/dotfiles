return {
  "sphamba/smear-cursor.nvim",
  opts = {
    -- stiffness = 0.8,       -- Cursor reaction speed (0.0-1.0)
    -- trailing_stiffness = 0.5,
    -- damping = 0.8,         -- Lower = more overshoot
  },
}
